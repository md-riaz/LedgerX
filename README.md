# LedgerX
Flutter offline-first ledger app using GetX (state, routing, DI). Local SQLite via sqflite_common_ffi; secure with AES file encryption. Features: customers, credit/debit entries, running balance, tags, quick search, bulk import CSV, PDF invoice/export, reminders via local_notifications, backup/restore, theme &amp; keyboard shortcuts.
