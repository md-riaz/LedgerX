# Assets

- `icons/` and `images/` contain placeholder `.gitkeep` files so Flutter's asset directory declarations resolve without warnings.
- `fonts/` bundles the Roboto Regular and Bold font files referenced in `pubspec.yaml`.
